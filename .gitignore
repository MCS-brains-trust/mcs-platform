# Python
__pycache__/
*.py[cod]
*.so
*.egg-info/
dist/
build/
venv/
.venv/

# Django
db.sqlite3
db.sqlite3-journal
/media/
/staticfiles/

# Environment
.env
.env.docker
*.env.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
docker-compose.override.yml
test.db
test.db-journal

# Intermediate text extracts
*.txt
handiledger_reference/_pl_table.txt
handiledger_reference/_scarft.txt
handiledger_reference/_scarft_raw.txt

# Stale nested repo snapshot — never tracked
mcs-platform/
archives/
test_fixtures/*.pdf

# Data-repair snapshots hold live client transaction detail — keep them on the
# server that ran the repair, never in the repository.
data_fixes/*snapshot*.json

# E2E test rig — local DB dumps, credentials, generated media, auth state
.e2e/
e2e/node_modules/
e2e/test-results/
e2e/playwright-report/
e2e/.auth/

# Claude Code worktrees — isolated checkouts, never part of the repo
.claude/worktrees/
