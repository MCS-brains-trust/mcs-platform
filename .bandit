# Bandit configuration
# B608 (hardcoded_sql_expressions) produces false positives in eva_graph.py
# and eva_retrieval.py which build SQL from internal constants only —
# no user input reaches these query strings; all values use %s placeholders.
skips:
  - B608
